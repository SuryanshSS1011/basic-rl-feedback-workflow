# Multi-LLM Benchmark Additional Dependencies
# Install with: pip install -r requirements.txt
# (After activating the main virtual environment)

# Dataset loading
datasets>=2.14.0
huggingface-hub>=0.19.0

# Data processing and metrics
pandas>=2.0.0
numpy>=1.24.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# Progress tracking
tqdm>=4.65.0

# HTTP requests (for APPS_Plus dataset)
requests>=2.31.0

# Note: The following are already installed via gpu_requirements.txt:
# - torch
# - transformers
# - accelerate
